;;; ****
(require "comm")

(defun webengine++lisp-webfetcher (num url &key dest-path (want-string nil)) ;(setq num 1) (setq url "http://www.betonverein.de")
; input:   Seitennummer (Zahl),
;          URL-Adresse (String),
; effect:  HTML-Dokument wird über einen Socket-Stream in eine Datei heruntergeladen.
; value:   nil, falls die Seite nicht heruntergeladen werden konnte.  

  (let ((host (url++get-host url)) ;(setq host (url++get-host url))
        (page (url++get-page url)))) ;(setq page (url++get-page url))
    "(Klammer in String)"
    ;; Socket wird erzeugt
;!!!!!!!!!!!!!!Hier fehlt eine Klammer!!!!!!!!!!!!!!!!!

toplevel-symbol

"String" ;TL-String zaehlt nicht als Symbol